package lecture;

    public class rectangle extends demo {

    public rectangle() {
                    }
    public rectangle(int length, int width, int height) {
        super();
        this.lenght = length;
        this.width = width;
        this.height = height;
    }

        @Override
        int peri(int length, int width) {
            return 0;
        }

        @Override
        int area(int length, int width) {
            return 0;
        }

        @Override
        int volume(int length, int width, int height) {
            return 0;
        }
    }
